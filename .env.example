# =============================================================================
# Neurodex Bot Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control!
#
# For detailed setup instructions, see: docs/deployment.md
# =============================================================================

# -----------------------------------------------------------------------------
# Application Environment
# -----------------------------------------------------------------------------

# Environment mode: 'development' or 'production'
# Development enables verbose logging and debug features
ENVIRONMENT=development

# Node.js environment (affects various packages' behavior)
# See: https://nodejs.org/en/learn/getting-started/nodejs-the-difference-between-development-and-production
NODE_ENV=production

# -----------------------------------------------------------------------------
# Telegram Bot Configuration
# -----------------------------------------------------------------------------

# Bot token from @BotFather on Telegram
# Create a new bot: https://t.me/BotFather -> /newbot
# Get token: /token command or during bot creation
TELEGRAM_BOT_TOKEN=

# -----------------------------------------------------------------------------
# Blockchain RPC URLs
# -----------------------------------------------------------------------------
# Use reliable RPC providers for production (Alchemy, Infura, QuickNode)
# Free tier usually has rate limits - consider paid plans for production

# Ethereum Mainnet RPC URL
# Example providers: Alchemy, Infura, QuickNode
ETHEREUM_MAINNET_RPC=https://your-ethereum-mainnet-rpc-url

# Base Mainnet RPC URL (Primary chain for trading)
# Base is an L2 built on Ethereum with lower fees
BASE_MAINNET_RPC=https://your-base-mainnet-rpc-url

# Base Sepolia Testnet RPC URL (for development/testing)
BASE_SEPOLIA_RPC=https://your-base-sepolia-rpc-url

# BNB Smart Chain (BSC) RPC URL
# Public RPC: https://bsc-dataseed.binance.org (has rate limits)
BNC_RPC=https://your-bnc-rpc-url

# -----------------------------------------------------------------------------
# Trading Configuration
# -----------------------------------------------------------------------------

# Default slippage tolerance (percentage, e.g., 1 = 1%)
# Higher slippage = more likely to execute but potentially worse price
# Recommended: 1-3% for stable pairs, 3-5% for volatile tokens
DEFAULT_SLIPPAGE=1

# Platform fee percentage charged on trades
# This fee goes to DEFAULT_FEE_WALLET
DEFAULT_FEE=1

# Wallet address to receive trading fees
# Must be a valid Ethereum address (0x...)
DEFAULT_FEE_WALLET=

# Default gas priority: 'standard' | 'fast' | 'instant'
# - standard: Normal speed, lower cost
# - fast: Faster confirmation, higher cost
# - instant: Fastest, highest cost
DEFAULT_GAS_PRIORITY=medium

# -----------------------------------------------------------------------------
# Database Configuration (Prisma + PostgreSQL)
# -----------------------------------------------------------------------------

# Prisma Accelerate connection string
# Sign up at: https://www.prisma.io/data-platform/accelerate
# Format: prisma+postgres://accelerate.prisma-data.net/?api_key=YOUR_KEY
#
# Accelerate provides:
# - Connection pooling
# - Edge caching
# - Global distribution
DATABASE_URL="prisma+postgres://accelerate.prisma-data.net/?api_key="

# Field-level encryption key for sensitive database fields
# Generate using: npx @47ng/cloak generate
# This encrypts sensitive data at rest (e.g., transaction details)
# IMPORTANT: Keep this key safe! Lost key = unrecoverable encrypted data
PRISMA_FIELD_ENCRYPTION_KEY=""

# -----------------------------------------------------------------------------
# Supabase Configuration (Private Key Storage)
# -----------------------------------------------------------------------------
# Supabase is used for secure, isolated private key storage
# Get credentials from: https://app.supabase.com/ -> Project Settings -> API

# Your Supabase project URL
SUPABASE_URL=https://your-project.supabase.co

# Anonymous public key (safe to expose in frontend)
# Used for unauthenticated read operations
SUPABASE_ANON_KEY=your-anon-key

# Service role key (NEVER expose publicly!)
# Has full database access, bypasses Row Level Security
# Used for server-side operations only
SUPABASE_SERVICE_KEY=your-service-key

# -----------------------------------------------------------------------------
# Encryption Keys
# -----------------------------------------------------------------------------

# Master password for encrypting/decrypting private keys
# CRITICAL: Use a strong, random password (minimum 32 characters)
# Generate with: openssl rand -hex 32
# NEVER share or commit this value!
MASTER_ENCRYPTION_PASSWORD=

# Secondary encryption key for wallet-level encryption
# Adds an additional layer of security for stored wallet data
# Generate with: openssl rand -hex 32
WALLET_ENCRYPTION_KEY=

# -----------------------------------------------------------------------------
# External API Keys
# -----------------------------------------------------------------------------

# CovalentHQ API key for blockchain data
# Used for portfolio tracking and token balances
# Sign up at: https://www.covalenthq.com/
COVALENTHQ_API_KEY=""

# -----------------------------------------------------------------------------
# Logging & Monitoring (BetterStack / Logtail)
# -----------------------------------------------------------------------------
# BetterStack provides centralized logging and monitoring
# Sign up at: https://betterstack.com/

# Source token from BetterStack Logs
# Create a source at: Logs -> Sources -> Create Source
BETTERSTACK_SOURCE_TOKEN=""

# BetterStack ingestion endpoint
# Default: https://in.logs.betterstack.com
BETTERSTACK_ENDPOINT=""
